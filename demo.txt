# Create a new local directory and navigate into it
mkdir my-new-project
cd my-new-project

# Initialize the directory as a Git repository
git init

# Create a sample file (e.g., a README)
echo "This is a sample project." >> README.md

# Stage the new file for commit
git add README.md

# Commit the staged file with a descriptive message
git commit -m "Initial commit with README"

# Link your local repository to a new remote repository on 
